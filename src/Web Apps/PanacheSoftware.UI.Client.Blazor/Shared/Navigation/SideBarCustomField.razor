<BarItem>
    <BarDropdown>
        <BarDropdownToggle>
            <BarIcon IconName="@("fa-file-code")" />
            @LanguageHelper.GetTitle(langQueryList, 10124)
        </BarDropdownToggle>
        <BarDropdownMenu>
            <BarDropdownItem To="taskgroup/taskgroups">
                @LanguageHelper.GetTitle(langQueryList, 10125)
            </BarDropdownItem>
            <BarDropdownItem To="taskgroup/createoredit">
                @LanguageHelper.GetTitle(langQueryList, 10126)
            </BarDropdownItem>
        </BarDropdownMenu>
    </BarDropdown>
</BarItem>

@code {
    [CascadingParameter(Name = "langQueryList")] LangQueryList langQueryList { get; set; }
}
