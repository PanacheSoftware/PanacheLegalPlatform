<BarItem>
    <BarDropdown>
        <BarDropdownToggle>
            <BarIcon IconName="@("fa-tools")" />
            @LanguageHelper.GetTitle(langQueryList, 10118)
        </BarDropdownToggle>
        <BarDropdownMenu>
            <BarDropdownItem To="setting/systemsetting">
                @LanguageHelper.GetTitle(langQueryList, 10123)
            </BarDropdownItem>
            <BarDropdownItem To="setting/language">
                @LanguageHelper.GetTitle(langQueryList, 10119)
            </BarDropdownItem>
            <BarDropdownItem To="setting/usersetting">
                @LanguageHelper.GetTitle(langQueryList, 10122)
            </BarDropdownItem>
        </BarDropdownMenu>
    </BarDropdown>
</BarItem>

@code {
    [CascadingParameter(Name = "langQueryList")] LangQueryList langQueryList { get; set; }
}
