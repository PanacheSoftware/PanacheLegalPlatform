<BarItem>
    <BarDropdown>
        <BarDropdownToggle>
            <BarIcon IconName="@("fa-users")" />
            @LanguageHelper.GetTitle(langQueryList, 10115)
        </BarDropdownToggle>
        <BarDropdownMenu>
            <BarDropdownItem To="user/users">
                @LanguageHelper.GetTitle(langQueryList, 10116)
            </BarDropdownItem>
            <BarDropdownItem To="user/index">
                @LanguageHelper.GetTitle(langQueryList, 10117)
            </BarDropdownItem>
        </BarDropdownMenu>
    </BarDropdown>
</BarItem>

@code {
    [CascadingParameter(Name = "langQueryList")] LangQueryList langQueryList { get; set; }
}
